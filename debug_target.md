# Debug 与优化目标确认单

> 日期：2026-07-31  
> 状态：RTL/MATLAB 修改、自检与最终布局布线均已完成；新 bit/ltx 待上板回归  
> 原则：先定位板级测量异常，再改变 FIR 和显示；每项修改都必须经过 MATLAB、XSim、
> Vivado 时序和板级复测闭环。

## 1. 赛题硬指标

依据《周期信号测量分析装置（G题）》：

| 项目 | 题目要求 |
|---|---|
| 信号组成 | 基波与 1 个或 2 个谐波组成的周期信号 |
| 任务 1 | 总 Vpp 100～250 mV，所有分量 10～200 kHz |
| 任务 2 | 总 Vpp 50～250 mV，所有分量 10～500 kHz |
| 任务 3 | 在任务 2 信号上叠加 200 mVpp、频率不低于 1 MHz 的单音干扰 |
| 时间 | 每项操作从启动装置起不超过 2 s |
| 时域 | 键控显示 1 个或 3 个完整周期 |
| 数值 | 总 Vpp、真有效值、基波频率、各频率分量的频率和幅值 |
| 频谱 | 定性显示正频率轴上的离散谱线 |
| 电压误差 | 总 Vpp、真有效值及各分量幅值绝对误差均不超过 5 mV |
| 频率误差 | 基波频率绝对误差不超过 1 kHz |
| 频率分辨率 | 500 Hz |
| 接口 | 50 Ω 信号源、50 Ω 同轴系统与 BNC 输入 |
| 供电/屏幕 | 单路 +5 V；显示屏不小于 6 英寸 |

当前 TJC8048X270_11、800×256 波形控件、R19 单次发送和三种图形模式满足基本
交互方向。500 kHz 是任务 2/3 的合法通带端点，必须稳定测准。

## 2. FIR 事实与调整目标

### 2.1 500 kHz 异常不是现有 FIR 的正常衰减

当前 FIR 确实是：

```text
20 MSPS
255 tap
Kaiser beta = 7.86
理想 sinc 截止参数 fc = 750 kHz
系数 signed Q1.17
```

这里的 750 kHz 是窗函数法中理想 sinc 的截止参数，不是“500 kHz 开始截止”。
现有定点分析报告给出：

```text
0～500 kHz 通带纹波约 0.00195 dB
500 kHz 增益约 -0.00101 dB
1 MHz 起最差阻带约 -76.7 dB
```

`-0.00101 dB` 对幅值的影响约为 0.012%，100 mV 峰值仅约 0.012 mV，无法解释
500 kHz 单音被测成 0～5 mV、400 kHz 伪峰或随机局部峰。因此，必须先查清
500 kHz 端点、帧通路或 ADC 相干采集问题，不能靠提高 FIR 截止频率掩盖。

### 2.2 提高截止频率的候选

使用当前 255 tap、Kaiser beta=7.86 的浮点设计函数做初扫：

| fc | 0～500 kHz 纹波 | 500 kHz 增益 | ≥1 MHz 最差阻带 |
|---:|---:|---:|---:|
| 750 kHz | 0.00155 dB | -0.00118 dB | -82.2 dB |
| 775 kHz | 0.00092 dB | -0.00029 dB | -80.4 dB |
| 800 kHz | 0.00097 dB | +0.00086 dB | -80.6 dB |
| 825 kHz | 0.00111 dB | +0.00080 dB | -53.4 dB |
| 850 kHz | 0.00068 dB | -0.00012 dB | -39.6 dB |

该表只是浮点候选扫描，和定点报告的网格、量化条件不同，不能直接替换系数。
`800 kHz` 已选为正式参数；`825 kHz` 以上会明显牺牲 1 MHz 阻带，不采用。

最终 Q1.17 定点分析结果为：0～500 kHz 波纹 `0.001899 dB`、500 kHz 增益
`+0.000095 dB`、1 MHz 起最差阻带 `-73.347 dB`、32 bit 累加器足够、系数对称
误差 0 LSB。

正式调整要求：

1. 重新生成 800 kHz 浮点系数和 Q1.17 系数；
2. 修正中心抽头，保持整数系数和严格等于 `2^17`；
3. 检查对称性、32 bit 累加器安全性、通带纹波和 ≥1 MHz 最差阻带；
4. 重生 FIR、端到端、FFT 测量向量；
5. 对 10、200、450、490、499、500 kHz 和 1、1.1、2、5 MHz 强制回归；
6. 只有当量化后 ≥1 MHz 最差阻带优于 -60 dB，且板测 500 kHz 稳定时才采用。

注意：数字 FIR 位于第一次 200→20 MSPS 抽取之后。它能滤除抽取后仍落在数字高频
区域的干扰，但不能恢复第一次无抗混叠抽取时已经混叠到 0～500 kHz 的干扰。任务 3
若要对任意 `fj≥1 MHz` 都可靠，最终仍需要 50 Ω 模拟低通/抗混叠前端。

## 3. 必须解决的功能性 Bug

### P0-1：500 kHz 通带端点测量偶发或持续失效

已观测：

- 偶尔 500 kHz/200 mVpp 可测为约 499.684 kHz/Um 104 mV；
- 校准后曾被拆成约 400 kHz/92 mV 和 500 kHz/8 mV；
- 严格执行关输出、改频、等待、开输出、再等待后，连续三次仍出现
  405.261 kHz/0 mV、406.262 kHz/0 mV、500.050 kHz/0 mV；
- 另一轮得到总 Vpp 200 mV、总 Vrms 4 mV，三个分量为
  81.540 kHz/1 mV、418.941 kHz/1 mV、499.999 kHz/5 mV；
- 小于但接近 500 kHz 的频点通常正常。

当前判断：

```text
20 MSPS FIR 输出
├─ 时域 max-min：Vpp 约 200 mV，基本正确
└─ /10 → 帧缓冲 → Hann → FFT → 峰搜索：相干能量异常
```

校准不是主因，因为时域 Vpp 和频谱幅值使用同一个增益。优先检查：

1. 500 kHz 位于搜索上边界 bin 1024，峰值判定是否始终取得 bin 1025 右护栏；
2. 精确边界、略低于边界、略高于边界以及不同初相位下的局部峰判定；
3. 第二级 `/10` 是否漏样、重复样或抽取相位异常；
4. 4096 点 ping-pong 帧是否覆盖、跨帧或错误释放；
5. FFT `valid/ready/last`、自然顺序 bin 和 block exponent 是否同帧；
6. ADC 200 MSPS 并行码是否因输入眼图边缘出现偶发 bit 错误。

验收：

- 490、495、499、499.5、499.9、500.0 kHz，50/200/250 mVpp；
- 每个组合改变初相位并连续测量至少 20 次；
- 不出现 400 kHz 附近伪峰，不出现随机双峰；
- 频率误差目标不超过 100 Hz，Um 误差目标不超过 2 mV；
- 总 Vpp、频谱 Um 和 Vrms 必须互相自洽。

### P0-2：ADC 板级相干采集风险未排除

当前 `ADC CLKOUT` 使用非专用时钟脚 L20，并设置
`CLOCK_DEDICATED_ROUTE FALSE`；输入延时参数仍是暂定假设。实现时序通过不等于
ADC 数据眼已经居中。

目标：

1. 临时诊断 ILA 同时观察 ADC 原始码、FIR 样点、FFT 输入样点和关键握手；
2. 在正常与异常 500 kHz 状态下各导出一次 CSV；
3. 检查原始码是否存在跳码、重复/漏样式相位突变；
4. 若失真从 ADC 原始码开始，评估 MMCM 相移/IDELAY 扫描数据眼；
5. 若 ADC/FIR 正常而 FFT 输入异常，转查抽取和帧缓冲；
6. 若 FFT 输入正常，转查 FFT 协议与谱分析器。

在定位前，不把任何新的 bitstream 宣称为正式稳定版本。

### P0-3：屏幕模式切换偶发失灵或需要按两次

现象：

- 进入频谱图后有时无法一次返回单周期/三周期；
- 最新测试出现波形模式切换按钮失灵。

目标握手：

```text
触屏命令到达
→ 锁存模式和绘图请求
→ 绘图模块明确 accept
→ 等待 render_done
→ 图形快照
→ 发送透明传输命令
→ 等待 FE
→ 发送 800 字节
→ 等待 FD
→ 完成；任一等待超时都返回 IDLE 并保留错误标志
```

要求：

- 请求必须保持到 `accept`，不能用可能丢失的单周期脉冲；
- 同时只允许一个活动事务，后续请求采用一深度 pending 或明确覆盖策略；
- `FE/FD/render_done` 每一级都有超时退出，不允许永久等待；
- 超时后下一次按键能够恢复；
- 连续快速点击 20 次，不死锁、不混合两种图形、不需要补按第二次。

## 4. 显示与交互优化

### P1-1：时域波形阶梯明显

500 kHz 在 20 MSPS 显示支路只有 40 个真实点/周期。当前最近邻重采样把这些样点
扩展到 800 点，因而形成台阶。

改为流水化线性插值：

\[
y=x_0+\alpha(x_1-x_0),\quad 0\leq\alpha<1
\]

只修改屏幕图形，不修改测量数据。要求：

- 500 kHz 单周期和三周期肉眼连续、无长平台；
- 首尾点不越界；
- 插值、缩放和 BRAM 读取分级流水；
- 重新实现后 200 MHz WNS/TNS 必须非负。

### P1-2：频谱左右边界峰只能看到一半

当前 0～500 kHz 直接映射到屏幕列 0～799，落在边界的谱线被控件裁掉。

拟在 800 点中保留左右各 24 点空白：

```text
列 0～23       ：0
列 24～775     ：映射 0～500 kHz
列 776～799    ：0
```

显示仍为定性频谱，数值频率不受影响。验收时对接近 10 kHz 和 500 kHz 的谱线检查
完整可见，且左右裕量对称。

### P1-3：频率只保留两位小数

频率控件 `x3/x5/x7` 改为显示 `kHz` 且小数点后两位：

```text
FPGA 发送值 = round(frequency_hz / 10)
屏幕虚拟浮点小数位 = 2
500000 Hz → 发送 50000 → 显示 500.00 kHz
12345 Hz  → 发送 1235  → 显示 12.35 kHz
```

内部频率仍保留整数 Hz，不因显示位数降低算法精度。README、屏幕测试指南和控件配置
必须同步修改。

### P1-4：发送新图前明确刷新

在透明传输前加入与屏幕控件兼容的清图/刷新过程，避免旧频谱与新时域图在环形写指针
上连续追加。该步骤必须与 `FE/FD` 透明传输握手分别管理，不能引入新的等待死锁。

## 5. 弱基波与相位问题

已观测测试：

```text
基波 50 mVpp，相位 100°
二次谐波 200 mVpp，相位 40°
四次谐波 250 mVpp，相位 0°
```

理论分量 Um 应约为 25、100、125 mV，但弱基波曾测得约 30 mV；取消相移后也可能
短暂偏差，随后输入较大单音再返回该组合又可能恢复正常。

从数学上，当前峰搜索使用

\[
|X[k]|^2=\operatorname{Re}(X[k])^2+\operatorname{Im}(X[k])^2
\]

理应与初相位无关。因此暂不直接增加“相位补偿系数”，先执行：

1. MATLAB 和 XSim 增加 0～359° 相位扫描；
2. 覆盖强弱比至少 1:2、1:5、1:10；
3. 覆盖基波+2次+4次、基波+3次+5次以及非零直流；
4. 检查低于最强峰 `1/2048` 功率门限附近的分量；
5. 检查信号源谐波发生模式下 Vpp 的定义、50 Ω 设置和切换瞬态；
6. 对连续多帧结果增加稳定性判断，拒绝改频/开关通道造成的过渡帧。

若纯数字仿真复现，修正峰值精修或采用“FFT 粗测频 + 已知频率处复数投影/Goertzel”
精修弱分量；若仿真始终正确而板上异常，则优先处理 ADC 采集和信号源状态，不修改
相位无关的幅值公式。

验收目标：任意初相位下，25 mV 弱基波误差不超过 2 mV，且结果不依赖先前是否输入
过大幅值单音。

## 6. 已解决问题与非 Bug，避免重复返工

| 项目 | 状态 |
|---|---|
| 10 kHz 曾误判约 16.7 kHz | 已通过搜索下界调整到 bin 20 修正，需保留回归 |
| Hann 窗导致幅值衰减 | 已有相干增益和非整数 bin scalloping 修正，需保留回归 |
| 300 kHz 等非整数 bin 测频 | 已有 Hann 三点插值，板测曾正常 |
| 三个 ILA 打包字段难辨认 | 已拆为独立 bin/幅值探针 |
| 多谐波整体 Vpp 不等于各 Um 简单相加 | 不是 Bug；整体 Vpp 由相位相关时域 max-min 得到 |
| 不同相位谐波可否分离 | 理论上可以；FFT 复数模长与相位无关，弱分量异常另行回归 |
| `control[2]` 并非恒为 1 | 不是 Bug；它是特定时刻的 valid/状态位 |

## 7. 超指标可发挥的方向

以下按投入产出比排序，先保证三项基本功能，再选择实现。

### 7.1 多帧稳定判决与过渡帧拒绝

- 连续 2～3 帧的分量数、频率和幅值一致后才更新显示；
- 改频、开关通道或校准后的混合帧不进入结果；
- 保留最后一帧可信结果，并设置内部 `measurement_confident`；
- 在 2 s 限时内仍有充足余量。

这项最能改善现场观感和可重复性。

### 7.2 FFT 粗定位 + 窄带精修

- FFT 负责找到最多三个候选频率；
- 对每个候选用 Goertzel、复数相关或少量频点 DFT 精修；
- 提升弱分量、非整数 bin 和相位变化下的幅值稳定性；
- 目标频率误差 ≤100 Hz、幅值误差 ≤2 mV。

### 7.3 分段频响校准

- 一点校准只校正整体比例；
- 在 10、100、200、300、400、500 kHz 建立小型 `K(f)` 表；
- 线性插值补偿模拟前端、ADC 和数字 FIR 的残余幅频响应；
- 校准仍可现场执行，不要求掉电保存。

### 7.4 ADC 采样相位自检

- 用 MMCM 相移或 IDELAY 扫描返回时钟/数据眼；
- 统计各相位下码流连续性和 FFT 杂散；
- 选择眼图中心，而不是依赖暂定 input delay；
- 可显著提升200 MSPS接口的工程完成度和答辩说服力。

### 7.5 可追溯自检与健康状态

- sticky error 区分 FIFO、帧、FFT 协议、UART 和校准错误；
- 保存最近一次有效帧号、分量数和置信状态；
- 提供固定虚拟输入回归以及一键 XSim 脚本；
- 测试报告保存输入设置、bit/ltx版本、ILA CSV和误差表。

### 7.6 更好的定性图形

- 时域线性插值；
- 频谱边界留白；
- 可选对数幅度显示或固定动态范围，弱谐波更容易看见；
- 图形快照后再发送，避免前后半帧撕裂。

## 8. 建议实施顺序

1. 冻结当前已上板基线，记录 bit/ltx、Git 状态和现有时序报告；
2. 补齐 500 kHz 上边界、弱基波相位扫描和UART请求握手的自检；
3. 生成临时诊断 ILA，定位 ADC/FIR/帧/FFT 中第一次失真的位置；
4. 修复 500 kHz 和屏幕模式请求问题；
5. 加入多帧稳定判决；
6. 将 FIR 改为经定点验证的 800 kHz 候选并完整回归；
7. 加入时域线性插值、频谱左右各 24 点留白和两位小数显示；
8. 若相位扫描在数字链复现，再实现窄带精修；否则不无依据修改算法；
9. 跑全部 MATLAB/XSim；
10. 综合、实现、DRC和时序检查；2026-07-31 最终 WNS 为 +0.059 ns、WHS 为
    +0.034 ns，任何后续改动仍需重跑；
11. 生成新的 bit/ltx 和板测指南；
12. 按任务 1、2、3 全量上板回归并形成误差表。

### 8.1 本轮实际完成状态

- 500 kHz 上边界改为允许最后合法 bin 1024 作为单边局部峰；
- 十帧 FFT 自检覆盖 499.5/499.9/500 kHz、不同相位和弱基波三音，全部通过；
- 新增连续两帧稳定门，拒绝改频/开关通道产生的首个过渡帧；
- 时域显示改为相邻样点线性插值，测量数据通路保持不变；
- 频谱映射改为列 24～775，左右各留 24 点；
- 频率改为 kHz 两位小数，传输前清图，并为 FE/FD 增加超时回收；
- ILA 改为 ADC/FIR/FFT 原始数据、FFT bin 和 32 bit 诊断状态，不再重复屏幕数值；
- MATLAB、ADC、FIR、帧、Hann/FFT 输入、十帧频谱、校准、稳定门、两种显示、
  UART 和端到端处理链自检均已通过。
- Vivado 2020.2 最终实现 WNS `+0.059 ns`、WHS `+0.034 ns`，未约束内部路径为 0，
  DRC 为 `0 Error / 0 Critical Warning`；bit/ltx 已生成。

## 9. 最终验收矩阵

| 类别 | 测试点 |
|---|---|
| 单音频率 | 10、13、20、50、100、200、300、450、490、499、499.5、499.9、500 kHz |
| 单音幅值 | 50、100、200、250 mVpp |
| 三音 | 基波+1个谐波、基波+2个谐波；强弱比至 1:10 |
| 相位 | 0°、40°、90°、100°、180°、270°及随机相位 |
| 干扰 | 1、1.1、2、5 MHz，200 mVpp |
| 切换 | 关输出改频、开输出、校准前后、连续快速按键 |
| 显示 | 单周期、三周期、频谱；500 kHz 右边界谱线完整 |
| 时间 | 从发起测量到可信显示小于 2 s |
| 正确性 | 基本指标：电压≤5 mV、频率≤1 kHz；冲刺目标：电压≤2 mV、频率≤100 Hz |
| 稳定性 | 每个关键组合连续20次，无伪峰、死锁、第二次按键补救或历史状态依赖 |

## 10. 当前未知项与风险

1. 无正式模拟抗混叠滤波器，任务 3 对任意 ≥1 MHz 干扰仍有物理风险；
2. ADC返回时钟位于非专用时钟脚，数据眼尚未实测；
3. 信号源谐波模式的幅值/相位切换瞬态和波形定义需要用示波器交叉确认；
4. 当前实现虽已时序收敛，但建立裕量仅 `+0.059 ns`；后续显示、握手或算法改动
   必须流水化并重新实现；
5. 频率显示两位小数只是显示格式，不能作为 10 Hz 测量精度声明；
6. 800 kHz FIR 已完成 Q1.17 量化、RTL 向量和数字回归，仍需本轮 bitstream 上板
   扫频确认。
